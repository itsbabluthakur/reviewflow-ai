{
  "name": "@reviewflow/errors",
  "version": "0.1.0",
  "private": true,
  "description": "Shared error hierarchy and API response helpers used across ReviewFlow AI apps and packages.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rimraf .turbo dist node_modules"
  },
  "devDependencies": {
    "@types/node": "^26",
    "eslint": "^10",
    "typescript": "^7",
    "vitest": "^4.1.10"
  }
}
