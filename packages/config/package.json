{
  "name": "@reviewflow/config",
  "version": "0.1.0",
  "private": true,
  "description": "Shared runtime configuration and environment variable helpers for ReviewFlow AI apps and packages.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "clean": "rimraf .turbo dist node_modules"
  },
  "devDependencies": {
    "@types/node": "^20",
    "eslint": "^9",
    "typescript": "^5"
  },
  "dependencies": {
    "@reviewflow/errors": "workspace:*",
    "zod": "^4.4.3"
  }
}
