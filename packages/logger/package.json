{
  "name": "@reviewflow/logger",
  "version": "0.1.0",
  "private": true,
  "description": "Shared structured logging (Pino) and request-context propagation for ReviewFlow AI apps and packages.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "lint": "eslint .",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "clean": "rimraf .turbo dist node_modules"
  },
  "dependencies": {
    "pino": "^9.6.0"
  },
  "devDependencies": {
    "@types/node": "^20",
    "eslint": "^9",
    "pino-pretty": "^13.0.0",
    "typescript": "^5",
    "vitest": "^4.1.10"
  }
}
